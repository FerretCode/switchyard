query Service($serviceId: String!) {
  service(id: $serviceId) {
    id
    deployments {
      edges {
        node {
          id
          status
          statusUpdatedAt
          createdAt
          updatedAt
        }
      }
    }
  }
}
